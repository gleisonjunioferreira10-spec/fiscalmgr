DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/gerente_fiscal

# Obrigatório. Gere um valor único por ambiente, nunca reutilize entre dev/staging/produção:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=
